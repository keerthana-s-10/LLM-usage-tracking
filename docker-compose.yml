services:
  db:
    image: postgres:17
    container_name: llm_usage_db
    environment:
      POSTGRES_DB: llm_usage
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: